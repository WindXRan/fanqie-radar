[build-system]
requires = ["setuptools>=69.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fanqie-radar"
version = "0.2.1"
description = "番茄雷达 Fanqie Radar — 番茄小说榜单数据 / 扫榜选书工作台 / MCP Server（fanqie novel rank data, webnovel trend analysis, imitation book scoring）。零配置、零爬虫、零第三方运行时依赖，内置当天公开榜单真实快照装完即用，附极速一键扫榜与一键安装 Agent Skill。"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = []
keywords = ["fanqie", "番茄小说", "番茄", "榜单", "扫榜", "网文", "网文写作", "小说", "webnovel", "novel", "rank", "mcp", "mcp-server", "model-context-protocol", "data-analysis", "选书", "仿写", "agent-skill", "claude-code", "codex", "ai-writing", "webnovel-skills", "写作文案"]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
fanqie-radar = "fanqie_index.mcp_server:main"
fanqie-radar-web = "fanqie_index.web:main"
fanqie-radar-scrape = "fanqie_index.scraper:main"
fanqie-radar-skill-install = "fanqie_index.install_skill:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
fanqie_index = ["sample_data/*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --tb=short"
